# Public Sanitization Notes

This public copy is prepared from a local working project with runtime traces removed.

## Removed from public copy
- absolute local paths
- runtime request / receipt JSONs
- session-demo artifacts and receipt-digest artifacts
- Python cache directories

## Rewritten for public examples
- `source.sessionId` → `public-demo`
- local workspace paths → relative project paths
- channel examples use `demo` instead of live chat channel names

## Kept intentionally
- public project pages
- templates and rendering scripts
- source-of-truth project/edition JSON
- examples that still explain the publish flow
